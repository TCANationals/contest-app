{
  "name": "@tca-timer/shared",
  "version": "0.0.1",
  "private": true,
  "description": "Shared timer-display logic consumed by the TCA Timer desktop overlay and judge SPA. Pure TypeScript, no runtime deps.",
  "type": "module",
  "main": "./src/index.ts",
  "module": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "files": [
    "src"
  ],
  "scripts": {
    "lint": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.6.2",
    "vitest": "^2.1.2"
  }
}
