export function TimerPage() {
  // TODO(§10.4): timer controls (−5 / −1 / +1 / +5), start/pause/stop,
  // connected-client count, 30s undo affordance.
  return (
    <section>
      <h1>Timer</h1>
      <p>Scaffold placeholder.</p>
    </section>
  );
}
