{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions the contestant overlay WebView needs.",
  "windows": ["overlay"],
  "permissions": [
    "core:default",
    "core:event:default"
  ]
}
